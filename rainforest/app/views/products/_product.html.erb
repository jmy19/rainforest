	<h2><%= link_to "#{product.name}", "/products/#{product.id}" %></h2>
	<p><%= product.description %></p>
	<p><%= product.formatted_price %> </p>
	<p> <%= link_to "Edit Product", "/products/#{product.id}/edit" %></p>
